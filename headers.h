#include <stdio.h>
#include <stdlib.h>
#include <math.h>

void stream( ?????? );
void collide( ????? );
void solid_BC( ???? );
void in_BC( ??????? );
void ex_BC_crude(?? );
void init_f(float*, float*, float*, float*, float*, float*,
	float*, float*, float*, int*, int, float, float);